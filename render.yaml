services:
  - type: web
    name: astrovoice-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main_openai_realtime:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false
    healthCheckPath: /health
